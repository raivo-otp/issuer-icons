os: linux
language: python

python:
  - 3.6

before_install:
  - sudo apt-get update
  - sudo apt-get install -y cairo libffi-dev

install:
  - pip install -r requirements.txt

script:
  - python validate.py